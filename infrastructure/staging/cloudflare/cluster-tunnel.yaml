# clustertunnel.yaml
apiVersion: networking.cfargotunnel.com/v1alpha1
kind: ClusterTunnel
metadata:
  name: cluster-tunnel
spec:
  newTunnel:
    name: staging-cluster
  size: 1
  cloudflare:
    domain: decentm.com
    secret: cloudflare-secrets
    accountName: DecentM
