apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - secrets
  - cnpg.yaml
  - redis.yaml
  - nutritrack.yaml
  - db-init-job.yaml
  - ingress.yaml

configMapGenerator:
  - name: nutritrack-config
    namespace: app-nutritrack
    envs:
      - config/nutritrack.env
