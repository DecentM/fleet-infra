apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: anytype-coordinator-networkstore
  namespace: app-anytype
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-local
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: anytype-coordinator
  namespace: app-anytype
  labels:
    app: anytype-coordinator
spec:
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: anytype-coordinator
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: anytype-coordinator
    spec:
      securityContext:
        fsGroup: 1000
      initContainers:
        - name: wait-for-mongo
          image: busybox:1.37.0
          imagePullPolicy: IfNotPresent
          command:
            - sh
            - -c
            - |
              until nc -z anytype-mongo.app-anytype.svc 27017; do
                echo "waiting for mongo..."
                sleep 2
              done
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
          resources:
            requests:
              cpu: 5m
              memory: 16Mi
            limits:
              cpu: 100m
              memory: 32Mi
      containers:
        - name: coordinator
          image: ghcr.io/anyproto/any-sync-coordinator:v0.9.1
          imagePullPolicy: IfNotPresent
          args:
            - -c
            - /config/config.yml
          ports:
            - name: grpc
              containerPort: 4001
              protocol: TCP
            - name: quic
              containerPort: 4011
              protocol: UDP
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            tcpSocket:
              port: grpc
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 12
          livenessProbe:
            tcpSocket:
              port: grpc
            initialDelaySeconds: 60
            periodSeconds: 15
            failureThreshold: 5
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
          volumeMounts:
            - name: config
              mountPath: /config
              readOnly: true
            - name: networkstore
              mountPath: /networkStore
      volumes:
        - name: config
          secret:
            secretName: anytype-coordinator-config
        - name: networkstore
          persistentVolumeClaim:
            claimName: anytype-coordinator-networkstore
      dnsConfig:
        options:
          - name: ndots
            value: "1"
---
apiVersion: v1
kind: Service
metadata:
  name: anytype-coordinator
  namespace: app-anytype
spec:
  type: ClusterIP
  selector:
    app: anytype-coordinator
  ports:
    - name: grpc
      port: 4001
      targetPort: grpc
      protocol: TCP
    - name: quic
      port: 4011
      targetPort: 4011
      protocol: UDP
---
apiVersion: v1
kind: Service
metadata:
  name: anytype-coordinator-ts
  namespace: app-anytype
  labels:
    tailscale.com/proxy-class: privileged-proxy
  annotations:
    tailscale.com/hostname: anytype-coordinator
spec:
  type: LoadBalancer
  loadBalancerClass: tailscale
  selector:
    app: anytype-coordinator
  ports:
    - name: grpc
      port: 4001
      targetPort: grpc
      protocol: TCP
    - name: quic
      port: 4011
      targetPort: 4011
      protocol: UDP
