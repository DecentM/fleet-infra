apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - secrets
  - cnpg.yaml
  - redis.yaml
  - tika.yaml
  - gotenberg.yaml
  - paperless.yaml
  - ingress.yaml

configMapGenerator:
  - name: paperless-config
    namespace: app-paperless-ngx
    envs:
      - config/paperless.env
