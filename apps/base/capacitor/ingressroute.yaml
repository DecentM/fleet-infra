apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: capacitor
  namespace: flux-system

spec:
  entryPoints:
    - web

  routes:
    - match: HostRegexp(`^capacitor\..*`)
      kind: Rule
      services:
        - name: capacitor
          port: 9000
