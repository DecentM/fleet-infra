apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - chart.yaml
  - ingressroute.yaml
configMapGenerator:
  - name: kubevious-values
    files:
      - values.yaml=values.yaml
