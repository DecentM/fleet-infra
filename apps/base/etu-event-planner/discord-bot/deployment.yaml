apiVersion: apps/v1
kind: Deployment

metadata:
  name: discord-bot
  namespace: app-etu-event-planner

spec:
  replicas: 1
  selector:
    matchLabels:
      app: discord-bot
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: discord-bot
    spec:
      containers:
        - name: discord-bot
          image: ghcr.io/eesti-transinimeste-uhing/event-manager:3a9dd2f2afd2a0c60aec1104c9d68c913db349a5
          command:
            - /usr/local/bin/node
          args:
            - /app/apps/discord-bot/src/index.js
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: discord-bot-config
            - secretRef:
                name: secrets
          ports:
            - containerPort: 3001
