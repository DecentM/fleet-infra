apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - livekit.yaml
  - lk-jwt-service.yaml
  - sealed-livekit-secrets.yaml
  - ingressroute.yaml
  - tunnel.yaml

configMapGenerator:
  - name: livekit-config
    namespace: app-matrix
    files:
      - config/livekit.yaml
