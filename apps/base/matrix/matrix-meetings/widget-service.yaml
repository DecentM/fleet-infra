# matrix-meetings Widget ClusterIP Service
# Exposes the widget frontend
---
apiVersion: v1
kind: Service
metadata:
  name: matrix-meetings-widget
  namespace: app-matrix
  labels:
    app: matrix-meetings-widget
    app.kubernetes.io/name: matrix-meetings-widget
    app.kubernetes.io/component: meetings-widget
    app.kubernetes.io/part-of: matrix
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  selector:
    app: matrix-meetings-widget
