# https://docs.mau.fi/meowlnir/
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: integrations-meowlnir
  namespace: app-matrix
  labels:
    app: integrations-meowlnir
    app.kubernetes.io/name: integrations-meowlnir
    app.kubernetes.io/component: moderation
    app.kubernetes.io/part-of: matrix
spec:
  replicas: 1
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: integrations-meowlnir
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: integrations-meowlnir
        job: integrations-meowlnir
        app.kubernetes.io/name: integrations-meowlnir
    spec:
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      initContainers:
        - name: wait-for-db
          image: busybox:1.37.0
          imagePullPolicy: IfNotPresent
          command:
            - sh
            - -c
            - |
              until nc -z integrations-meowlnir-db-rw.app-matrix.svc 5432; do
                echo "Waiting for Meowlnir database..."
                sleep 2
              done
              echo "Database is ready"
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
        - name: config-templating
          image: oaudry/envsubst:1.6
          imagePullPolicy: IfNotPresent
          command:
            - sh
            - -c
            - |
              echo "Processing config templates with envsubst..."
              for file in /config-template/*; do
                filename=$(basename "$file")
                echo "Processing $filename..."
                envsubst < "$file" > "/config-processed/$filename"
              done
              echo "Config templating complete"
              ls -la /config-processed/
          env:
            - name: MEOWLNIR_AS_TOKEN
              valueFrom:
                secretKeyRef:
                  name: integrations-meowlnir-secrets
                  key: meowlnir_as_token
            - name: MEOWLNIR_HS_TOKEN
              valueFrom:
                secretKeyRef:
                  name: integrations-meowlnir-secrets
                  key: meowlnir_hs_token
            - name: MEOWLNIR_MANAGEMENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: integrations-meowlnir-secrets
                  key: meowlnir_management_secret
            - name: MEOWLNIR_DATA_SECRET
              valueFrom:
                secretKeyRef:
                  name: integrations-meowlnir-secrets
                  key: meowlnir_data_secret
            - name: MEOWLNIR_PICKLE_KEY
              valueFrom:
                secretKeyRef:
                  name: integrations-meowlnir-secrets
                  key: meowlnir_pickle_key
            - name: MEOWLNIR_DB_USER
              valueFrom:
                secretKeyRef:
                  name: integrations-meowlnir-db-app
                  key: user
            - name: MEOWLNIR_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: integrations-meowlnir-db-app
                  key: password
            - name: MEOWLNIR_DB_NAME
              valueFrom:
                secretKeyRef:
                  name: integrations-meowlnir-db-app
                  key: dbname
            # Cross-namespace: reads main server's DB for efficient redaction
            - name: SYNAPSE_DB_USER
              valueFrom:
                secretKeyRef:
                  name: matrix-db-app
                  key: user
            - name: SYNAPSE_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: matrix-db-app
                  key: password
            - name: SYNAPSE_DB_NAME
              valueFrom:
                secretKeyRef:
                  name: matrix-db-app
                  key: dbname
          volumeMounts:
            - name: config-template
              mountPath: /config-template
              readOnly: true
            - name: config-processed
              mountPath: /config-processed
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
            allowPrivilegeEscalation: false
      containers:
        - name: meowlnir
          image: dock.mau.dev/maunium/meowlnir:v0.2602.0
          imagePullPolicy: IfNotPresent
          command:
            - /usr/bin/meowlnir
          args:
            - --config
            - /config/meowlnir-config.yaml
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
            allowPrivilegeEscalation: false
          ports:
            - name: antispam
              containerPort: 29339
              protocol: TCP
            - name: management
              containerPort: 29340
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /_matrix/mau/ready
              port: antispam
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /_matrix/mau/live
              port: antispam
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          volumeMounts:
            - name: config-processed
              mountPath: /config
            - name: data
              mountPath: /data
      volumes:
        - name: config-template
          configMap:
            name: integrations-meowlnir-config
        - name: config-processed
          emptyDir: {}
        - name: data
          persistentVolumeClaim:
            claimName: integrations-meowlnir-data
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: integrations-meowlnir-data
  namespace: app-matrix
  labels:
    app: integrations-meowlnir
    app.kubernetes.io/name: integrations-meowlnir
    app.kubernetes.io/component: moderation
    app.kubernetes.io/part-of: matrix
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-local
  resources:
    requests:
      storage: 1Gi
