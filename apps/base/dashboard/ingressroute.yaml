apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: dashboard
  namespace: apps

spec:
  entryPoints:
    - web

  routes:
    - match: HostRegexp(`^dashboard\..*`)
      kind: Rule
      services:
        - name: kubernetes-dashboard-web
          port: 8000
