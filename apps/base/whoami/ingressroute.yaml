apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: whoami
  namespace: whoamiv1

spec:
  entryPoints:
    - web
  routes:
    - match: Host(`whoami.cluster.decentm.local`)
      kind: Rule
      services:
        - name: mirror1
          namespace: default
          kind: TraefikService
