apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - announcer.yaml

configMapGenerator:
  - name: announcer-config
    namespace: app-minecraft
    envs:
      - config/announcer.env
