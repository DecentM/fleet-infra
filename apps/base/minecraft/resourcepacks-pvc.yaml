apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: resourcepacks
  namespace: app-minecraft
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn-local
