apiVersion: apps/v1
kind: StatefulSet

metadata:
  name: fallback
  namespace: app-minecraft

spec:
  replicas: 1
  selector:
    matchLabels:
      app: fallback
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: fallback
    spec:
      containers:
        - name: server
          image: ghcr.io/decentm/minecraft-servers:fallback-d7084923541e1554e70defa951ec7cebcfa4bb83
          securityContext:
            capabilities:
              add:
                - CAP_NET_RAW
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: fallback-config
          ports:
            - containerPort: 25565
          volumeMounts:
            - name: logs
              mountPath: /data/logs
            - name: config
              mountPath: /data/ops.json
              subPath: ops.json
      volumes:
        - name: logs
          persistentVolumeClaim:
            claimName: fallback-logs
        - name: config
          configMap:
            name: fallback-config-files
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fallback-logs
  namespace: app-minecraft
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: longhorn-local
