apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: opencode
  namespace: app-opencode

spec:
  entryPoints:
    - websecure

  routes:
    - match: Host(`opencode.cluster.arpa`)
      kind: Rule
      services:
        - name: opencode
          namespace: app-opencode
          port: http
  tls:
    secretName: traefik-tls
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: traefik-cert
  namespace: app-opencode
spec:
  secretName: traefik-tls
  duration: 672h # 28d
  renewBefore: 168h # 7d
  issuerRef:
    name: custom-ca-cluster-issuer
    kind: ClusterIssuer
  commonName: "opencode.cluster.arpa"
  dnsNames:
    - "opencode.cluster.arpa"
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: traefik-ingress-controller
  namespace: app-opencode

roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: traefik-ingress-controller
subjects:
  - kind: ServiceAccount
    name: traefik-ingress-controller
    namespace: traefik-system
