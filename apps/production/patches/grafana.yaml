apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: grafana
  namespace: app-o11y
spec:
  values:
    grafana.ini:
      server:
        domain: grafana.cluster.arpa
        root_url: http://grafana.cluster.arpa
        serve_from_sub_path: false
